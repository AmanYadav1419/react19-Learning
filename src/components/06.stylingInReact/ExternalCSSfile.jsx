// this is file for understanding how to apply external styling using css file to react component.
// you can import this component to App component to check the result of this component.

import "./index.css";

const ExternalCSSfile = () => {
  return (
    <section>
      <h1>External CSS file to understand how to apply css externally.</h1>
    </section>
  );
};

export default ExternalCSSfile;
