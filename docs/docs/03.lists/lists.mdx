## Lists

- In React, you will render lists with some type of loop.
- The Javascript map() array method is generally the preferred method.

Here is the example of the Map method used in jsx 👇

```jsx
const ListExample = () => {
  const numbers = [1, 2, 3, 4, 5];
  return (
    <main>
      {numbers.map((number) => (
        <ul key={number}>
          <li>{number}</li>
        </ul>
      ))}
    </main>
  );
};

export default ListExample;
```

- remember the point each child in a list should have unique "key" prop.

Second Example of the Map method used in jsx 👇

- now we want to iterate all the user details in the userInfo array so,
  in this we are mapping an array of objects

```jsx
const ListExample1 = () => {
  const userInfo = [
    {
      username: "Aman",
      email: "aman@gmail.com",
      location: "India",
    },
    {
      username: "Yadav",
      email: "yadav@gmail.com",
      location: "Bharat",
    },
    {
      username: "Trion",
      email: "trion@gmail.com",
      location: "Hindustan",
    },
  ];

  return (
    <main>
      {userInfo.map((user) => (
        <ul key={user.email}>
          <li>{user.username}</li>
          <li>{user.location}</li>
          <li>{user.email}</li>
        </ul>
      ))}
    </main>
  );
};

export default ListExample1;
```

## Excercise : Rendering a List Data with .map()

- in this excercise, you will learn how to render a list items using the .map() method in JSX.

Step 1 : Create a UserList Component.

1. Create a new file called UserList.jsx.
2. Inside this file, Create a functional component called UserList.
3. In the component, Create a users array with the following objects represents user with id, name and age.

```jsx
const users = [
  { id: 1, name: "Aman", age: 22 },
  { id: 2, name: "Trion", age: 25 },
  { id: 3, name: "Yash", age: 26 },
];
```

4. Use the .map() method to render a list of users. Each user's name and age should be displayed inside a <div> element. Use the id as the unique key for each list item.

Step 2 : Create a ProductList Component.

1. Create a new file called ProductList.jsx.
2. Inside this file, Create a functional component called ProductList.
3. Create a products array with the following objects, where each object represents a product with id, name and price.

```jsx
const products = [
  { id: 1, name: "SmartPhone", price: "$699" },
  { id: 2, name: "Laptop", price: "$1200" },
  { id: 3, name: "Headphones", price: "$100" },
];
```

4. Use the .map() method to render a list of users. Each product's name and price should be displayed inside a <div> element. Use the id as the unique key for each list item.

Step 3 : Render the Components in App.jsx

1. In your App.jsx file, import the UserList and ProductList components

```jsx
import UserList from "./components/UserList";
import ProductList from "./components/ProductList";
```

2. Inside the App component's return statement, render both the UserList and ProductList Components:

```jsx
function App() {
  return (
    <div>
      <UserList />
      <ProductList />
    </div>
  );
}

export default App;
```
